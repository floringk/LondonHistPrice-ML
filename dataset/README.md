# Dataset (local only)

Place `kaggle_london_house_price_data.csv` here before running the pipeline.

Source: Kaggle dataset `jakewright/house-price-data` (file name `kaggle_london_house_price_data.csv`). Raw CSV files under `dataset/` are gitignored due to size.
